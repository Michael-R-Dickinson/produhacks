---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/pyproject.toml
  - agents/__init__.py
  - agents/models/__init__.py
  - agents/models/requests.py
  - agents/models/responses.py
  - agents/mocks/__init__.py
  - agents/mocks/portfolio.py
  - agents/mocks/news.py
  - agents/mocks/modeling.py
  - agents/mocks/alternatives.py
  - agents/bridge/__init__.py
  - agents/bridge/events.py
  - agents/tests/__init__.py
  - agents/tests/test_models.py
  - agents/tests/test_mock.py
  - .env.example
  - .env
  - .gitignore
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "All 4 request models and 4 response models are importable from agents.models"
    - "Each mock function returns a valid instance of its corresponding response model"
    - "MOCK_DATA env var is documented and loadable"
  artifacts:
    - path: "agents/models/requests.py"
      provides: "AnalyzePortfolio, FetchNews, RunModel, AnalyzeAlternatives"
      exports: ["AnalyzePortfolio", "FetchNews", "RunModel", "AnalyzeAlternatives"]
    - path: "agents/models/responses.py"
      provides: "PortfolioResponse, NewsResponse, ChartOutput, ModelResponse, AlternativesResponse"
      exports: ["PortfolioResponse", "NewsResponse", "ChartOutput", "ModelResponse", "AlternativesResponse"]
    - path: "agents/mocks/portfolio.py"
      provides: "mock_portfolio_response()"
      exports: ["mock_portfolio_response"]
    - path: "agents/mocks/news.py"
      provides: "mock_news_response()"
      exports: ["mock_news_response"]
    - path: "agents/mocks/modeling.py"
      provides: "mock_model_response()"
      exports: ["mock_model_response"]
    - path: "agents/mocks/alternatives.py"
      provides: "mock_alternatives_response()"
      exports: ["mock_alternatives_response"]
    - path: "agents/bridge/events.py"
      provides: "push_event(), cross-loop event queue helpers"
      exports: ["push_event"]
  key_links:
    - from: "agents/mocks/*.py"
      to: "agents/models/responses.py"
      via: "import and instantiate response models"
      pattern: "from agents\\.models\\.responses import"
---

<objective>
Scaffold the project, define all Pydantic message contracts, create mock data fixtures, and set up the event bridge module.

Purpose: Establish the typed contracts that every agent and the bridge depend on. Models-first means Plan 02 can implement agents and bridge against stable interfaces.
Output: Installable Python package with message models, mock fixtures, event bridge module, and passing unit tests.
</objective>

<execution_context>
@/Users/big_m/.claude/get-shit-done/workflows/execute-plan.md
@/Users/big_m/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffold, Pydantic message models, and event bridge module</name>
  <files>
    agents/pyproject.toml,
    agents/__init__.py,
    agents/models/__init__.py,
    agents/models/requests.py,
    agents/models/responses.py,
    agents/bridge/__init__.py,
    agents/bridge/events.py,
    agents/tests/__init__.py,
    agents/tests/test_models.py,
    .env.example,
    .env,
    .gitignore
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2: Cross-Loop Event Push, Pattern 4: Pydantic Message Models)
  </read_first>
  <action>
    1. Create `.gitignore` with standard Python entries: `__pycache__/`, `*.pyc`, `.venv/`, `.env`, `*.egg-info/`, `dist/`, `.pytest_cache/`

    2. Create `agents/pyproject.toml` with project metadata and dependencies:
       ```toml
       [project]
       name = "investiswarm"
       version = "0.1.0"
       requires-python = ">=3.11"
       dependencies = [
           "uagents==0.24.0",
           "fastapi==0.115.*",
           "uvicorn==0.30.*",
           "sse-starlette==3.3.3",
           "python-dotenv==1.*",
           "httpx==0.27.*",
       ]

       [project.optional-dependencies]
       dev = ["pytest", "pytest-asyncio", "httpx"]

       [tool.pytest.ini_options]
       asyncio_mode = "auto"
       testpaths = ["tests"]
       ```

    3. Create `.env.example` with:
       ```
       MOCK_DATA=true
       ```

    4. Create `.env` with:
       ```
       MOCK_DATA=true
       ```

    5. Create `agents/__init__.py` — empty file.

    6. Create `agents/models/__init__.py` that re-exports all models:
       ```python
       from agents.models.requests import AnalyzePortfolio, FetchNews, RunModel, AnalyzeAlternatives
       from agents.models.responses import PortfolioResponse, NewsResponse, ModelResponse, AlternativesResponse
       ```

    7. Create `agents/models/requests.py` — All request models extend `uagents.Model`:
       - `AnalyzePortfolio`: `holdings: list[str]` (ticker symbols), `mock: bool = False`
       - `FetchNews`: `tickers: list[str]`, `mock: bool = False`
       - `RunModel`: `holdings: list[str]`, `analyses: list[str] = ["regression"]`, `lookback_days: int = 365`, `mock: bool = False`
       - `AnalyzeAlternatives`: `mock: bool = False`

       Each class has a docstring indicating direction (e.g., "Orchestrator -> Portfolio Agent").

    8. Create `agents/models/responses.py` — All response models extend `uagents.Model`:
       - `PortfolioResponse`: `sector_allocation: dict[str, float]`, `top_holdings: list[dict]`, `herfindahl_index: float`, `portfolio_beta: float`
       - `NewsResponse`: `headlines: list[dict]`, `aggregate_sentiment: dict[str, float]`, `overall_sentiment: float`
       - `ChartOutput`: `chart_type: str`, `title: str`, `image_base64: str`, `summary: str`
       - `ModelResponse`: `holdings_analyzed: list[str]`, `sharpe_ratio: float`, `volatility: float`, `trend_slope: float`, `charts: list[ChartOutput]`, `metrics: dict[str, float]`
       - `AlternativesResponse`: `crypto_prices: dict[str, float]`, `cross_correlations: dict[str, float]`

    9. Create `agents/bridge/__init__.py` — empty file.

    10. Create `agents/bridge/events.py` — Cross-loop event push utility:
        - Module-level `_fastapi_loop: asyncio.AbstractEventLoop | None = None`
        - Module-level `_event_queue: asyncio.Queue | None = None`
        - `push_event(agent_id: str, event_type: str, payload: dict[str, Any]) -> None` that uses `_fastapi_loop.call_soon_threadsafe(_event_queue.put_nowait, event)` where `event = {"agent_id": agent_id, "type": event_type, **payload}`
        - Guard: if `_fastapi_loop` or `_event_queue` is None, return silently

    11. Create `agents/tests/__init__.py` — empty file.

    12. Create `agents/tests/test_models.py`:
        - `test_all_request_models_importable`: import all 4 request classes from `agents.models.requests`, assert each is not None
        - `test_all_response_models_importable`: import all 4 response classes from `agents.models.responses`, assert each is not None
        - `test_request_roundtrip_serialization`: for each request model, create an instance with sample data, call `.model_dump()` then reconstruct from dict, assert fields match
        - `test_response_roundtrip_serialization`: for each response model, create an instance with sample data, call `.model_dump()` then reconstruct from dict, assert fields match
        - `test_models_reexported_from_init`: import all 8 models from `agents.models`, assert each is accessible

    13. Install dependencies: `cd agents && uv venv ../.venv && source ../.venv/bin/activate && uv pip install -e ".[dev]"`

    14. Run tests: `cd /Users/big_m/Documents/Code/produhacks && .venv/bin/pytest agents/tests/test_models.py -x -q`
  </action>
  <verify>
    <automated>.venv/bin/pytest agents/tests/test_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - agents/models/requests.py contains "class AnalyzePortfolio(Model):"
    - agents/models/requests.py contains "class FetchNews(Model):"
    - agents/models/requests.py contains "class RunModel(Model):"
    - agents/models/requests.py contains "class AnalyzeAlternatives(Model):"
    - agents/models/responses.py contains "class PortfolioResponse(Model):"
    - agents/models/responses.py contains "class NewsResponse(Model):"
    - agents/models/responses.py contains "class ChartOutput(Model):"
    - agents/models/responses.py contains "class ModelResponse(Model):"
    - agents/models/responses.py contains "class AlternativesResponse(Model):"
    - agents/models/__init__.py contains "from agents.models.requests import"
    - agents/models/__init__.py contains "from agents.models.responses import"
    - agents/bridge/events.py contains "def push_event("
    - agents/bridge/events.py contains "call_soon_threadsafe"
    - .env.example contains "MOCK_DATA=true"
    - agents/pyproject.toml contains "uagents==0.24.0"
    - agents/pyproject.toml contains 'asyncio_mode = "auto"'
    - agents/tests/test_models.py exits 0 via pytest
  </acceptance_criteria>
  <done>All 8 message models defined, importable, and round-trip serializable. Event bridge module created with push_event(). Test suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Mock data fixtures with unit tests</name>
  <files>
    agents/mocks/__init__.py,
    agents/mocks/portfolio.py,
    agents/mocks/news.py,
    agents/mocks/modeling.py,
    agents/mocks/alternatives.py,
    agents/tests/test_mock.py
  </files>
  <read_first>
    agents/models/responses.py (the response types each mock must return),
    .planning/phases/01-foundation/01-RESEARCH.md (Mock Data Fixture Structure section, Pattern 5)
  </read_first>
  <action>
    1. Create `agents/mocks/__init__.py` that re-exports all mock functions:
       ```python
       from agents.mocks.portfolio import mock_portfolio_response
       from agents.mocks.news import mock_news_response
       from agents.mocks.modeling import mock_model_response
       from agents.mocks.alternatives import mock_alternatives_response
       ```

    2. Create `agents/mocks/portfolio.py`:
       ```python
       from agents.models.responses import PortfolioResponse

       def mock_portfolio_response() -> PortfolioResponse:
           return PortfolioResponse(
               sector_allocation={
                   "Technology": 0.42,
                   "Healthcare": 0.18,
                   "Financials": 0.15,
                   "Consumer Discretionary": 0.12,
                   "Energy": 0.08,
                   "Other": 0.05,
               },
               top_holdings=[
                   {"ticker": "AAPL", "weight": 0.18, "sector": "Technology"},
                   {"ticker": "MSFT", "weight": 0.15, "sector": "Technology"},
                   {"ticker": "NVDA", "weight": 0.09, "sector": "Technology"},
                   {"ticker": "UNH", "weight": 0.08, "sector": "Healthcare"},
                   {"ticker": "JPM", "weight": 0.07, "sector": "Financials"},
               ],
               herfindahl_index=0.087,
               portfolio_beta=1.12,
           )
       ```

    3. Create `agents/mocks/news.py`:
       ```python
       from agents.models.responses import NewsResponse

       def mock_news_response() -> NewsResponse:
           return NewsResponse(
               headlines=[
                   {"title": "Apple reports record Q1 revenue driven by iPhone 16 demand", "sentiment": 0.82, "ticker": "AAPL"},
                   {"title": "Microsoft Azure growth slows amid enterprise spending pullback", "sentiment": -0.35, "ticker": "MSFT"},
                   {"title": "NVIDIA announces next-gen Blackwell Ultra chips for AI data centers", "sentiment": 0.91, "ticker": "NVDA"},
                   {"title": "JPMorgan warns of commercial real estate risks in Q2 outlook", "sentiment": -0.48, "ticker": "JPM"},
                   {"title": "UnitedHealth Group expands Medicare Advantage network coverage", "sentiment": 0.55, "ticker": "UNH"},
               ],
               aggregate_sentiment={"AAPL": 0.82, "MSFT": -0.35, "NVDA": 0.91, "JPM": -0.48, "UNH": 0.55},
               overall_sentiment=0.29,
           )
       ```

    4. Create `agents/mocks/modeling.py`:
       ```python
       from agents.models.responses import ChartOutput, ModelResponse

       def mock_model_response() -> ModelResponse:
           return ModelResponse(
               holdings_analyzed=["AAPL", "MSFT", "NVDA", "UNH", "JPM"],
               sharpe_ratio=1.34,
               volatility=0.187,
               trend_slope=0.0023,
               charts=[
                   ChartOutput(
                       chart_type="regression",
                       title="Portfolio Linear Regression (1Y)",
                       image_base64="",  # Phase 2 will generate real chart images
                       summary="Portfolio shows positive trend with R-squared 0.74 over 12 months",
                   ),
               ],
               metrics={"r_squared": 0.74, "max_drawdown": -0.12, "beta": 1.12},
           )
       ```

    5. Create `agents/mocks/alternatives.py`:
       ```python
       from agents.models.responses import AlternativesResponse

       def mock_alternatives_response() -> AlternativesResponse:
           return AlternativesResponse(
               crypto_prices={"BTC": 67450.0, "ETH": 3520.0, "SOL": 142.0, "FET": 2.35},
               cross_correlations={"BTC": 0.12, "ETH": 0.18, "GOLD": -0.05, "OIL": 0.08},
           )
       ```

    6. Create `agents/tests/test_mock.py`:
       - `test_mock_portfolio_returns_portfolio_response`: call `mock_portfolio_response()`, assert `isinstance(result, PortfolioResponse)`, assert `result.sector_allocation` has at least 3 keys, assert `result.herfindahl_index > 0`
       - `test_mock_news_returns_news_response`: call `mock_news_response()`, assert `isinstance(result, NewsResponse)`, assert `len(result.headlines) >= 3`, assert `-1.0 <= result.overall_sentiment <= 1.0`
       - `test_mock_model_returns_model_response`: call `mock_model_response()`, assert `isinstance(result, ModelResponse)`, assert `result.sharpe_ratio > 0`, assert `result.volatility > 0`, assert `len(result.charts) >= 1`, assert `result.charts[0].chart_type == "regression"`
       - `test_mock_alternatives_returns_alternatives_response`: call `mock_alternatives_response()`, assert `isinstance(result, AlternativesResponse)`, assert `"BTC" in result.crypto_prices`
       - `test_all_mocks_importable_from_init`: import all 4 mock functions from `agents.mocks`, call each, assert result is not None

    7. Run tests: `.venv/bin/pytest agents/tests/test_mock.py -x -q`
  </action>
  <verify>
    <automated>.venv/bin/pytest agents/tests/test_mock.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - agents/mocks/portfolio.py contains "def mock_portfolio_response() -> PortfolioResponse:"
    - agents/mocks/news.py contains "def mock_news_response() -> NewsResponse:"
    - agents/mocks/modeling.py contains "def mock_model_response() -> ModelResponse:"
    - agents/mocks/alternatives.py contains "def mock_alternatives_response() -> AlternativesResponse:"
    - agents/mocks/__init__.py contains "from agents.mocks.portfolio import mock_portfolio_response"
    - agents/tests/test_mock.py exits 0 via pytest
  </acceptance_criteria>
  <done>All 4 mock fixtures return valid response model instances. All mock tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite for Plan 01 artifacts:
```bash
.venv/bin/pytest agents/tests/test_models.py agents/tests/test_mock.py -x -q
```
All tests pass. Models importable, mocks return valid typed responses.
</verification>

<success_criteria>
- 8 Pydantic message models defined and importable from agents.models
- 4 mock functions return valid typed response instances
- push_event() bridge utility implemented with call_soon_threadsafe pattern
- pytest configured and all unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
