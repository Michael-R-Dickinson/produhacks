---
phase: 02-agent-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - agents/alternatives_agent.py
  - agents/modeling_agent.py
  - agents/tests/test_alternatives.py
autonomous: true
requirements:
  - ALT-01
  - ALT-02
  - ALT-03
  - ALT-04
  - MODL-01
  - MODL-02
  - MODL-04
  - MODL-05

must_haves:
  truths:
    - "Alt Assets agent fetches crypto prices from CoinGecko"
    - "Alt Assets agent fetches gold and oil prices from Finnhub"
    - "Alt Assets agent computes trend signals (bullish/bearish/neutral) per asset"
    - "Alt Assets agent fetches BTC dominance from CoinGecko global endpoint"
    - "Alt Assets agent computes cross-asset correlations with portfolio equity returns"
    - "Modeling agent returns enhanced mock with real base64 chart from Plan 01"
  artifacts:
    - path: "agents/alternatives_agent.py"
      provides: "Live CoinGecko + Finnhub commodity fetching and correlation logic"
      contains: "coingecko.com"
    - path: "agents/tests/test_alternatives.py"
      provides: "Unit tests for trend signal logic and correlation computation"
      contains: "def test_"
  key_links:
    - from: "agents/alternatives_agent.py"
      to: "https://api.coingecko.com/api/v3"
      via: "httpx async GET"
      pattern: "coingecko.com"
    - from: "agents/alternatives_agent.py"
      to: "https://finnhub.io/api/v1/quote"
      via: "httpx async GET for commodities"
      pattern: "finnhub.io"
---

<objective>
Implement the Alt Assets agent's live logic (CoinGecko crypto, Finnhub commodities, cross-asset correlations, trend signals) and update the Modeling agent's mock to use the real base64 chart from Plan 01.

Purpose: The Alt Assets agent provides the crypto/commodity dimension. Pairing it with the Modeling agent mock update keeps this plan focused on the two agents that don't need FinBERT or heavy computation.
Output: Working alternatives_agent.py with real API integrations, updated modeling_agent.py mock with real chart, unit tests.
</objective>

<execution_context>
@/Users/big_m/.claude/get-shit-done/workflows/execute-plan.md
@/Users/big_m/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-pipeline/02-CONTEXT.md
@.planning/phases/02-agent-pipeline/02-RESEARCH.md
@.planning/phases/02-agent-pipeline/02-01-SUMMARY.md

<interfaces>
From agents/models/responses.py (after Plan 01):
```python
class AlternativesResponse(Model):
    crypto_prices: dict[str, float]           # ticker -> USD price
    cross_correlations: dict[str, float]       # asset -> correlation with portfolio
    trend_signals: dict[str, str]              # asset -> "bullish" | "bearish" | "neutral"
    btc_dominance: float                       # BTC market cap as % of total crypto
    commodities: dict[str, float]              # "GOLD" -> price, "OIL" -> price

class ModelResponse(Model):
    holdings_analyzed: list[str]
    sharpe_ratio: float
    volatility: float
    trend_slope: float
    charts: list[ChartOutput]
    metrics: dict[str, float]
```

From agents/models/requests.py:
```python
class AnalyzeAlternatives(Model):
    mock: bool = False

class RunModel(Model):
    holdings: list[str]
    analyses: list[str] = ["regression"]
    lookback_days: int = 365
    mock: bool = False
```

From agents/data/portfolio.py (after Plan 01):
```python
EQUITY_TICKERS = [...]  # 10 equity tickers
CRYPTO_TICKERS = [...]  # ["BTC", "ETH"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Alt Assets agent with CoinGecko crypto and Finnhub commodities</name>
  <files>agents/alternatives_agent.py, agents/tests/test_alternatives.py</files>
  <read_first>agents/alternatives_agent.py, agents/models/responses.py, agents/data/portfolio.py, agents/mocks/alternatives.py</read_first>
  <behavior>
    - Test: `trend_signal(5.2)` returns "bullish", `trend_signal(-4.1)` returns "bearish", `trend_signal(1.5)` returns "neutral"
    - Test: `compute_cross_correlations(alt_returns, portfolio_returns)` returns dict with correlation values in [-1, 1]
    - Test: mock mode returns mock_alternatives_response() unchanged
  </behavior>
  <action>
1. Create `agents/tests/test_alternatives.py` with tests for pure logic functions. Tests must NOT call CoinGecko or Finnhub -- use pre-built mock data.

2. Add the following functions to `agents/alternatives_agent.py`:

   **CoinGecko ID mapping (module level):**
   ```python
   COINGECKO_IDS = {
       "BTC": "bitcoin", "ETH": "ethereum", "SOL": "solana",
       "BNB": "binancecoin", "AVAX": "avalanche-2",
   }
   ```

   **`async def fetch_crypto_prices(coins: list[str]) -> tuple[dict[str, float], dict[str, float]]`**
   - GET `https://api.coingecko.com/api/v3/simple/price?ids={ids}&vs_currencies=usd&include_24hr_change=true&include_7d_change=true`
   - Map CoinGecko response back to ticker symbols
   - Return (prices_dict, change_7d_dict): ({ticker: usd_price}, {ticker: 7d_percent_change})
   - Timeout: 10 seconds
   - Always include BTC and ETH regardless of portfolio crypto holdings

   **`async def fetch_btc_dominance() -> float`**
   - GET `https://api.coingecko.com/api/v3/global`
   - Return `data["data"]["market_cap_percentage"]["btc"]` as float
   - Timeout: 10 seconds

   **`async def fetch_commodity_prices(api_key: str) -> dict[str, float]`**
   - Use httpx to GET `https://finnhub.io/api/v1/quote?symbol=GC1!&token={api_key}` for gold
   - Use httpx to GET `https://finnhub.io/api/v1/quote?symbol=CL1!&token={api_key}` for oil
   - Extract current price from response `data["c"]`
   - Return `{"GOLD": gold_price, "OIL": oil_price}`
   - Timeout: 10 seconds per request

   **`def trend_signal(price_change_7d: float) -> str`**
   - If change > 3.0: return "bullish"
   - If change < -3.0: return "bearish"
   - Otherwise: return "neutral"

   **`def compute_cross_correlations(alt_prices_history: dict, equity_returns: pd.Series) -> dict[str, float]`**
   - For crypto: use yfinance to get BTC-USD, ETH-USD 90-day history, compute daily returns, correlate with a simple equal-weighted equity portfolio returns proxy
   - For commodities: use GLD (gold ETF) and USO (oil ETF) as proxies for correlation
   - Return {asset: correlation_coefficient}
   - Round to 4 decimal places

3. Update `handle_analyze_alternatives` handler's else branch:
   - Read FINNHUB_API_KEY from os.environ
   - Use asyncio.gather to fetch crypto prices, BTC dominance, and commodity prices concurrently
   - Compute trend_signals from 7d price changes
   - Compute cross_correlations
   - Emit 3-5 thought events (personality: market-savvy, crypto-native):
     - "Pulling crypto prices from CoinGecko..."
     - "BTC at $67,450 (+5.2% 7d) -- bullish signal" (use real values)
     - "BTC dominance: 52.3% -- altcoin rotation risk moderate"
     - "Gold $2,340, Oil $78 -- checking portfolio correlations..."
     - "Cross-asset correlations computed: BTC-portfolio 0.12, Gold-portfolio -0.05"
   - Construct and return AlternativesResponse

4. Run tests: `cd /Users/big_m/Documents/Code/produhacks/agents && python -m pytest tests/test_alternatives.py -x -v`
  </action>
  <verify>
    <automated>cd /Users/big_m/Documents/Code/produhacks/agents && python -m pytest tests/test_alternatives.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - agents/alternatives_agent.py contains "coingecko.com/api/v3"
    - agents/alternatives_agent.py contains "finnhub.io/api/v1/quote"
    - agents/alternatives_agent.py contains "def trend_signal"
    - agents/alternatives_agent.py contains "def fetch_crypto_prices"
    - agents/alternatives_agent.py contains "def fetch_btc_dominance"
    - agents/alternatives_agent.py contains "def fetch_commodity_prices"
    - agents/alternatives_agent.py contains "def compute_cross_correlations"
    - agents/alternatives_agent.py contains "FINNHUB_API_KEY"
    - agents/alternatives_agent.py contains "push_sse_event(SSEEvent.agent_thought"
    - agents/tests/test_alternatives.py contains "def test_trend_signal"
    - agents/tests/test_alternatives.py exits 0
  </acceptance_criteria>
  <done>Alt Assets agent fetches crypto from CoinGecko, commodities from Finnhub, computes trend signals and cross-asset correlations. Returns fully populated AlternativesResponse. Thought events are market-savvy with real data teasers.</done>
</task>

<task type="auto">
  <name>Task 2: Update Modeling agent mock with enhanced thought events</name>
  <files>agents/modeling_agent.py</files>
  <read_first>agents/modeling_agent.py, agents/mocks/modeling.py</read_first>
  <action>
The Modeling agent stays mock-only in Phase 2 (teammate handles real implementation). Update `handle_run_model` to emit richer thought events that match the analytical personality:

1. Update the thought events in `handle_run_model` (both mock and else branches since both use mock for now):
   - "Loading {lookback_days}-day price history for {len(holdings)} holdings..."
   - "Running {', '.join(analyses)} analysis..."
   - "Sharpe ratio: 1.34 -- above benchmark threshold"
   - "Volatility: 18.7% annualized -- moderate risk profile"
   - "Chart generated: Portfolio Linear Regression (1Y)"

2. Ensure the mock response from `mock_model_response()` now includes the real base64 chart (from Plan 01 update to `agents/mocks/modeling.py`). No code change needed if Plan 01 updated it correctly -- just verify the import chain works.

3. Verify: `cd /Users/big_m/Documents/Code/produhacks/agents && python -c "from agents.modeling_agent import modeling_agent; from agents.mocks.modeling import mock_model_response; r = mock_model_response(); assert len(r.charts[0].image_base64) > 100; print('Modeling mock OK')"`
  </action>
  <verify>
    <automated>cd /Users/big_m/Documents/Code/produhacks/agents && python -c "from agents.mocks.modeling import mock_model_response; r = mock_model_response(); assert len(r.charts[0].image_base64) > 100; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - agents/modeling_agent.py contains "Sharpe ratio"
    - agents/modeling_agent.py contains "Volatility"
    - agents/modeling_agent.py contains "Chart generated"
    - Mock model response has non-empty image_base64 (len > 100)
  </acceptance_criteria>
  <done>Modeling agent emits analytical thought events with data teasers. Mock response includes real base64 PNG chart. Agent is drop-in ready for teammate's real implementation.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_alternatives.py -x -v` passes
- `python -c "from agents.alternatives_agent import trend_signal; assert trend_signal(5.0) == 'bullish'; assert trend_signal(-5.0) == 'bearish'; assert trend_signal(1.0) == 'neutral'; print('OK')"` passes
- Modeling mock chart base64 is non-empty
</verification>

<success_criteria>
Alt Assets agent fetches live crypto/commodity data and computes correlations. Modeling agent has rich thought events and real mock chart. Both agents return fully populated response contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-pipeline/02-04-SUMMARY.md`
</output>
